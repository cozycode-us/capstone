package cozycode.capstone.parking.car;

/*
 * CarType Enum
 * Specifies the different types of Cars and ParkingSpaces there can be
 */
public enum CarType {
    HANDICAP,
    OVERSIZE,
    EV,
    CARPOOL,
    REGULAR
}
